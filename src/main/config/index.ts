import { load } from './load';
import { get } from './get';
import { set } from './set';
import { data } from './data';
import { register } from './register';

export default { load, get, set, data, register };

export { load, get, set, data, register };
